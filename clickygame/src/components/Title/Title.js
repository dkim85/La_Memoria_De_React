import React from "react";
import "./Title.css";

const Title = props => (
    <div className="titlebox">
        <h1>South Park Clicky Game!</h1>
        <p>Click on a character to make points, but be careful not clicking it twice!</p>
    </div>
);
export default Title;